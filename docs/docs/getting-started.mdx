---
sidebar_position: 1
hide_title: true
custom_edit_url: null
title: "Getting Started"
---

<h1 style={{ display: "flex", alignItems: "center", justifyContent: "center", fontFamily: "Balsamiq sans" }}>
    <img width="10%" height="10%" src="/img/logo.svg" />
    <span style={{ marginLeft: "20px", paddingTop: "5px" }}>informel</span>
    {/* <h1 style='font-family: "Balsamiq Sans"'>informel</h1> */}
    {/* <span style='font-family: "Balsamiq Sans";'>informel</span> */}
</h1>

`informel` is a Web Component that wraps your native HTML forms and gives them super powers.  
Features:

✅ &nbsp;Easily obtain form values  
✅ &nbsp;Automatically show validation errors  
✅ &nbsp;Native & custom validation rules  
✅ &nbsp;Easily track form validity state through classes  
✅ &nbsp;Keep track of whether your form is dirty or not  
✅ &nbsp;**Auto submission via AJAX call, using `action` and `method` attributes**

## Installation

### CDN Installation

Add the following script to your HTML page

```html
<script src="https://unpkg.com/informel"></script>
```

You can then start using `<inform-el />` and `<inform-field />` elements anywhere in your HTML!.

### NPM/Yarn Installation

Install the package:

```bash
npm i informel
yarn add informel
```

Then import `informel` at the root level of your app:

```javascript
import "informel";
```

You can then start using `<inform-el />` and `<inform-field />` elements anywhere in your HTML!.

## Usage

### Basic Usage

Just wrap your native form in `<inform-el />`:

```html
<inform-el>
    <form>
        <input type="text" name="firstName" placeholder="First name" />
        <input type="text" name="lastName" placeholder="Last name" />
        <button type="submit">Submit</button>
    </form>
</inform-el>
```

:::tip Don't forget the `<form>` element as `<informel>` heavily relies on native form features!
:::

Then you can listen to the `submit` event on `<inform-el>` which gives you the form values:

```javascript
document.querySelector("inform-el").addEventListener("submit", ({ detail: { values } }) => {
    console.log("Submitted:", values);
});
```

<iframe width="100%" height="300" src="//jsfiddle.net/juliendelort/hkpm5b2n/10/embedded/result,html,js/"></iframe>

### Show validation errors

In order to automatically show validation errors, wrap your fields in `<inform-field />`.

```html
<inform-el>
    <form>
        <inform-field>
            <input type="text" name="firstName" placeholder="First name" required />
        </inform-field>
        <inform-field>
            <input type="text" name="lastName" placeholder="Last name" required />
        </inform-field>
        <button type="submit">Submit</button>
    </form>
</inform-el>
```

Try submitting the empty form below to see the errors:

<iframe width="100%" height="300" src="//jsfiddle.net/juliendelort/5Lfdzsew/4/embedded/result,html/"></iframe>

:::tip `informel` leverages [native form validation features](https://developer.mozilla.org/en-US/docs/Learn/Forms/Form_validation) and displays [native validation messages](https://developer.mozilla.org/en-US/docs/Web/API/HTMLObjectElement/validationMessage) by default.
:::

Here is a more elaborated example that showcases some of `informel` features:

<iframe width="100%" height="800" src="//jsfiddle.net/juliendelort/rwz9gxu2/93/embedded/result,html,js,css"></iframe>

:::tip You are still in full control of the styling and layout of your forms.
:::

On top of that, `informel` accepts custom validation rules, as well as custom error messages. For more information, see [error customization](/docs/error-customization).

## Send an Ajax request on submit

## Dirty check

## Form reset

## Getting form values

## Custom validation

## API (events, attributes and classes)

## Limitations

`<select multiple />` not supported.
